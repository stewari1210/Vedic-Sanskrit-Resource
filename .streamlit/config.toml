[client]
showErrorDetails = true

# No [theme] block: Streamlit follows the user's OS light/dark preference
# automatically. (A defined theme always overrides the system setting —
# the previous block hardcoded a light background.)

[logger]
level = "info"