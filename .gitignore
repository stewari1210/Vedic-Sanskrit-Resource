# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

output.log
# tests folder
tests/
CONVERSATION_CONTEXT.md
# corpus text + retrieval indexes ARE needed by the Streamlit Cloud deploy
# (BM25 pickle, corpus lexicon, and local_store markdown as self-heal source)
vector_store/*
!vector_store/ancient_history/
vector_store/ancient_history/*
!vector_store/ancient_history/docs_chunks.pkl
!vector_store/ancient_history/corpus_lexicon.pkl
# legacy corrupt PDF extracts — do not track
legacy_local_store_pdf_extracts/
library/
scripts/
upload_vector_to_Qdrant.py
# never commit secrets in ANY form (incl. backups)
secrets.toml*
*.toml.bak
.streamlit/secrets*
*.txt
!packages.txt
!requirements.txt
!requirements-frozen.txt
*.pdf
# --- archived build-time / dev-only material (kept locally, excluded from package) ---
archive/
__pycache__/
*.pyc
.DS_Store

# internal working doc (cluster IDs, incident postmortem) — keep out of the public package
PROJECT_STATUS.md
